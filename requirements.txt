# Model / inference
torch>=2.1
transformers>=4.40
sentencepiece>=0.2.0
protobuf>=4.25

# API server
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# (optional) Original Streamlit frontend — keep if you still want to run app.py
streamlit>=1.32
